language: node_js
node_js:
  - 5
services:
  - postgresql
before_script:
  - psql -f server/schema.sql
  - psql -c 'CREAT DATABASE travis;' -U postgres
  - node server/server.js &
  - postgres://postgres@localhost:5432/city_scavenge
